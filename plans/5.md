# Plan for #5: Enable the metrics endpoint in the deployment

## Goal
Enable the existing metrics listener in Kubernetes, expose it as a named `metrics` container port, and document port-forward access.

## Dependencies
- confirmed: `src/config.ts` and `src/server.ts` already support `METRICS_ENABLED` and `METRICS_PORT`; change: no
- confirmed: `k8s/configmap.yaml` disables metrics; change: yes
- confirmed: `k8s/deployment.yaml` declares only the HTTP container port; change: yes
- confirmed: `README.md` documents the environment variables but not access; change: yes

## Tests
- Pipeline rollout and smoke checks validate the deployment; no application unit behavior changes.

## Environment contract
Set existing `METRICS_ENABLED=true` and `METRICS_PORT=9100`; both remain documented in README.

## Risks
An incorrect port or missing config can crash the pod; rollout and smoke gates should catch this. Metrics access is pod-local and documented via port-forward.

## Open questions
None.

## Decisions
Expose metrics as container port `9100` named `metrics`; do not alter the existing HTTP Service.

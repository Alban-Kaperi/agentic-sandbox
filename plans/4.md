# Plan for #4: Filter runs by vehicle on the web page

## Goal
Add a labeled vehicle filter above the runs table. Typing filters visible rows client-side, clearing restores all rows, and adding a run preserves the active filter.

## Dependencies
- confirmed: `public/index.html` renders runs and reloads after creation; change: yes
- confirmed: `e2e/smoke.spec.ts` contains browser coverage for the page; change: yes
- confirmed: `src/config.ts`, `k8s/configmap.yaml`, and README environment table define the existing contract; change: no

## Tests
- `e2e/smoke.spec.ts`: filter to `WVW-1001`, assert only matching rows, and verify the filter remains applied after adding a run.

## Environment contract
No environment variables are added or changed.

## Risks
Filtering could display stale or nonmatching rows after reload; browser tests cover initial filtering and post-create reload. Unit, build, rollout, stability, smoke, and browser pipeline gates could fail if the page script or selectors regress.

## Open questions
None.

## Decisions
Filtering is client-side, as permitted by the ticket.
